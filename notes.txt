install ndoe js v16
npm install @angular/cli@16.1.6 --location=global
ng v

Create new project:	ng new appointment-app
Start the application: 	ng serve -o  
Create new component: 	ng g component appointment-list
Create new interface: 	ng g interface models/appointment

install 'angular language service' - for angular intelissense 

npm install bootstrap@5.3

-------------------------------------------------------------------------------------
To connect github
1. opt bash and generate the key in local machine
    ssh-keygen -t rsa -b 4096 -C "rgerero@gmail.com"
    ls -al ~/.ssh
2. copy the pub key
    clip < ~/.ssh/id_rsa2.pub

3. If you see origin, remove it:
    git remote -v
    git remote remove origin

4. Add you git repo then push
    git remote add origin https://github.com/rgerero/appointmentapp-ng.git
    git config --global http.postBuffer 209715200 
    git config --global http.postBuffer 524288000
    git push -u origin master    
    git push --all origin --force --thin (use this if getting error of hung state)

5. Or try below
    echo "# my-new-repo" >> README.md
    git init
    git add README.md
    git commit -m "first commit"
    git branch -M main
    git remote add origin https://github.com/username/my-new-repo.git
    git push -u origin main
6 if getting error error: 
    - failed to push some refs to 'https://github.com/rgerero/hotel-app-ng.git'
    -or fatal: refusing to merge unrelated histories

    git pull origin main --allow-unrelated-histories
    git push -u origin main

git remote set-url origin https://github.com/rgerero/appointmentapp-ng.git
git remote set-url origin git@github.com:rgerero/appointmentapp-ng.git

-------------------------------------------------------------------------------------
CHEAT CODE
ng new projectName - Creates a new Angular project with the specified project name.
ng serve - Builds the application and starts a web server to serve your application during development.
ng serve --open 
or
ng serve -o
 - Same as ng serve, but also opens your default web browser to the application.
ng generate component componentName
or
ng g c componentName
 - Generates a new component with the specified name.
ng generate service serviceName
or
ng g s serviceName
 - Generates a new service with the specified name.
ng build - Builds your application for production, creating a dist/ folder with the output.
ng update - Checks your application for outdated dependencies, and can also update them.